import React from "react";
import DashLayout from "../../utils/dash_layout";

const Reviews = (props) => {
  return (
    <DashLayout auth={props.auth} title="Profile">
      This is reviews
    </DashLayout>
  );
};

export default Reviews;
